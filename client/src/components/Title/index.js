import React from 'react';
import Typography from 'material-ui/Typography';

export default ({ children }) =>
  <Typography className="pt-4 pl-4" variant="headline" gutterBottom>{ children }</Typography>
